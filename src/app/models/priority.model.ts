export class Priority {
  public id: number;
  public title: string;
  public color: string;

  constructor(id: number, title: string, color: string) {
    this.id = id;
    this.title = color;
    this.color = color
  }
}
